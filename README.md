# Приложение "Персонажи Рик и Морти"

Мобильное приложение на Flutter для просмотра персонажей из мультсериала "Рик и Морти" с использованием публичного API [Rick and Morty API](https://rickandmortyapi.com/).

---

## Описание

Приложение позволяет:

- Просматривать список всех персонажей с пагинацией
- Добавлять персонажей в избранное
- Просматривать детальную информацию о персонаже
- Сортировать избранных персонажей
- Работать в оффлайн режиме благодаря кешированию
- Переключаться между светлой и темной темой

---

## Основные функции

### Главный экран

- **Список персонажей** с изображениями, именами и характеристиками
- **Пагинация** - автоматическая подгрузка при прокрутке
- **Pull-to-refresh** - обновление данных
- **Кнопка избранного** - добавление/удаление персонажа в избранное
- **Индикатор кеша** - показывает время последнего обновления
- **Анимации** - плавное появление карточек с каскадным эффектом

### Экран избранного

- **Список избранных персонажей**
- **Сортировка**:
  - По имени (А-Я / Я-А)
  - По статусу (живые первыми / мертвые первыми)
- **Удаление свайпом** - смахните карточку влево для удаления из избранного
- **Отмена удаления** - через SnackBar

### Детальный экран персонажа

- **Полная информация** о персонаже
- **Большое изображение** с градиентом
- **Информационные карточки** с данными:
  - Пол, тип, происхождение, локация
  - Количество эпизодов
  - Дата создания, ID
- **Кнопка избранного** в AppBar

### Дополнительные возможности

- **Темная тема** с переключателем в AppBar
- **Оффлайн работа** - кеширование данных и изображений
- **Анимации**:
  - Анимированная кнопка избранного (scale + rotation)
  - Плавное появление карточек (fade + slide)
  - Удаление свайпом (Dismissible)

---

## Технологии

### Зависимости

- **Flutter SDK**: `^3.9.2`
- **flutter_riverpod**: `^3.0.3` - state management
- **hooks_riverpod**: `^3.0.3` - hooks + riverpod
- **flutter_hooks**: `^0.21.3+1` - React hooks для Flutter
- **go_router**: `^16.3.0` - навигация
- **dio**: `^5.9.0` - HTTP клиент
- **shared_preferences**: `^2.5.3` - локальное хранилище
- **flutter_svg**: `^2.2.1` - SVG иконки
- **cached_network_image**: `^3.4.1` - кеширование изображений

### Архитектурные решения

- **State Management**: Flutter Riverpod 3.0
- **Навигация**: Go Router с StatefulShellRoute
- **Кеширование**: Cache-first стратегия
- **UI**: Material Design 3
- **Хуки**: Flutter Hooks для управления состоянием виджетов

## 🚀 Установка и запуск

### 1. Клонирование репозитория

```bash
git clone git@github.com:spaiki007/EffectiveMobile.git
cd EffectiveMobile
```

### 2. Установка зависимостей

```bash
flutter pub get
```

### 3. Проверка окружения

```bash
flutter doctor
```

Убедитесь, что все проверки пройдены успешно.

### 4. Запуск приложения

#### На эмуляторе/устройстве:

```bash
flutter run
```

#### Для конкретной платформы:

```bash
# Android
flutter run -d android

# iOS (только на macOS)
flutter run -d ios

# Web
flutter run -d chrome
```

### 5. Сборка релизной версии

#### Android APK:

```bash
flutter build apk --release
```

Готовый APK будет в `build/app/outputs/flutter-apk/app-release.apk`

#### iOS (только на macOS):

```bash
flutter build ios --release
```

---

## 📁 Структура проекта

```
lib/
├── classes/api/
│   └── rick_morty_api.dart          # API клиент для Rick and Morty API
├── models/
│   ├── character.dart               # Модель персонажа
│   └── character_response.dart      # Модель ответа API с пагинацией
├── riverpod/
│   ├── queries_provider.dart        # Основные провайдеры (API, токен)
│   ├── favorites_provider.dart      # Провайдеры избранного и сортировки
│   ├── cache_provider.dart          # Провайдеры кеширования
│   └── theme_provider.dart          # Провайдер темы
├── screens/
│   ├── home.dart                    # Главный экран со списком персонажей
│   ├── favorites_screen.dart        # Экран избранного
│   ├── character_detail_screen.dart # Детальная информация о персонаже
│   └── wrap.dart                    # Обертка с BottomNavigationBar
├── services/
│   ├── favorites_service.dart       # Сервис управления избранным
│   └── cache_service.dart           # Сервис кеширования данных
├── theme/
│   └── app_theme.dart               # Светлая и темная темы
├── widgets/
│   ├── character_card.dart          # Карточка персонажа с анимацией
│   ├── animated_favorite_button.dart # Анимированная кнопка избранного
│   └── bottom_navigation_items.dart # Элементы нижней навигации
├── main.dart                        # Точка входа приложения
└── router.dart                      # Конфигурация маршрутизации
```

---

## Особенности реализации

### State Management

- Использование **NotifierProvider** вместо устаревшего StateProvider (Riverpod 3.0+)
- Разделение логики на отдельные провайдеры (favorites, cache, theme)
- FutureProvider для асинхронных операций

### Кеширование

- **Cache-first стратегия**: сначала показываем кеш, затем обновляем с API
- **Кеширование данных**: сохранение в SharedPreferences в виде JSON
- **Кеширование изображений**: автоматическое кеширование через cached_network_image
- Индикатор времени последнего обновления
- Полная работа без интернета (данные + изображения)

### Анимации

- **AnimatedFavoriteButton**: elastic scale + rotation при добавлении в избранное
- **Fade + Slide**: плавное появление карточек с задержкой
- **Dismissible**: свайп для удаления из избранного

### Навигация

- **Go Router** с declarative routing
- **StatefulShellRoute** для сохранения состояния вкладок
- Nested routes для детальных экранов

## API

Приложение использует [Rick and Morty API](https://rickandmortyapi.com/documentation/):

- `GET /api/character?page={page}` - список персонажей
- `GET /api/character/{id}` - информация о персонаже
- `GET /api/character/{ids}` - множественные персонажи

## Автор

Тестовое задание выполнено для **Effective Mobile**.